import React from 'react';

const PlacedOrders = () => {
	// render placed orders

	// to be used in dashboard page!

	return (
		<div>orders placed by users, to keep track of sales, shipment etc...</div>
	);
};

export default PlacedOrders;
