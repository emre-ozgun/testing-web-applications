import React from 'react';

const Navbar = () => {
	// checkout link -> isAuth ? <Link to='/checkout/> : 'do not render'
	//

	return (
		<header>
			<nav>NAV</nav>
		</header>
	);
};

export default Navbar;
