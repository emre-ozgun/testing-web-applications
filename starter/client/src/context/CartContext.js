import React from 'react';

const CartContext = () => {
	//check cart in server
	// update cart item count in navbar
	// update cart in server (add item to cart POST)
	// update cart in server (update amount in cart POST);

	return <div></div>;
};

export default CartContext;
