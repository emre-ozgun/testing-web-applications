import React from 'react';

const CheckoutPage = () => {
	// isAuth -> useAuthProvider
	//if !isAuth -> redirect
	// if isAuth -> render

	return <div>checkout</div>;
};

export default CheckoutPage;
